<!-- constraint-equivalence-report -->

## Constraint Equivalence Report

**Last updated:** 2026-03-24 17:26 UTC  
**Range:** `f7c294c441..0216cd69de` (45 commits)

### Summary

| Metric | Value |
|--------|-------|
| Commits analyzed | 45 |
| Pairs with changes | **6** |
| Pairs verified equivalent | 38 |
| Total constraints updated | 98 |
| Total constraints added | 5 |

### Changes

#### `94b25974d5` refactor: thread ChipletSelectors through all constraint and bus functions ([diff](https://github.com/0xMiden/miden-vm/compare/1e23a431e2...94b25974d5))

> 18 updated | 447 unchanged

<details>
<summary>Show 18 changes</summary>

**Updated:**

| Expression | Remark |
|------------|--------|
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/ace.rs#L136) `builder.assert_zero(ace_last.clone() * sstart.clone());` | `ChipletSelectors` struct threaded as parameter. 18 constraints affected in 3 sub-groups: (A) 4 in ace.rs where inline `ace_flag * flag_ace_last` / `ace_flag * flag_ace_next` / `is_transition * ace_flag` products replaced by precomputed `ace_last` / `ace_transition`; (B) 1 in kernel_rom.rs where `when_transition()` folded into the precomputed `flag_next_row_first_kernel_rom` which now includes the transition factor; (C) 13 in ace.rs, kernel_rom.rs, memory.rs with identical source expressions — fingerprints changed because the scoped builder's gate was recomputed using `ChipletSelectors` products. All 18 are semantically equivalent. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/ace.rs#L138) `builder.assert_zero(ace_transition.clone() * sstart.clone() * sstart_next.clone());` | `ChipletSelectors` struct threaded as parameter. 18 constraints affected in 3 sub-groups: (A) 4 in ace.rs where inline `ace_flag * flag_ace_last` / `ace_flag * flag_ace_next` / `is_transition * ace_flag` products replaced by precomputed `ace_last` / `ace_transition`; (B) 1 in kernel_rom.rs where `when_transition()` folded into the precomputed `flag_next_row_first_kernel_rom` which now includes the transition factor; (C) 13 in ace.rs, kernel_rom.rs, memory.rs with identical source expressions — fingerprints changed because the scoped builder's gate was recomputed using `ChipletSelectors` products. All 18 are semantically equivalent. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/ace.rs#L142) `builder.assert_zero( ace_transition.clone() * f_next.clone() * sblock.clone() * sblock_next.not(), );` | `ChipletSelectors` struct threaded as parameter. 18 constraints affected in 3 sub-groups: (A) 4 in ace.rs where inline `ace_flag * flag_ace_last` / `ace_flag * flag_ace_next` / `is_transition * ace_flag` products replaced by precomputed `ace_last` / `ace_transition`; (B) 1 in kernel_rom.rs where `when_transition()` folded into the precomputed `flag_next_row_first_kernel_rom` which now includes the transition factor; (C) 13 in ace.rs, kernel_rom.rs, memory.rs with identical source expressions — fingerprints changed because the scoped builder's gate was recomputed using `ChipletSelectors` products. All 18 are semantically equivalent. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/ace.rs#L168) `builder.assert_zero(within_section_gate.clone() * (clk_next.clone() - clk.clone()));` | `ChipletSelectors` struct threaded as parameter. 18 constraints affected in 3 sub-groups: (A) 4 in ace.rs where inline `ace_flag * flag_ace_last` / `ace_flag * flag_ace_next` / `is_transition * ace_flag` products replaced by precomputed `ace_last` / `ace_transition`; (B) 1 in kernel_rom.rs where `when_transition()` folded into the precomputed `flag_next_row_first_kernel_rom` which now includes the transition factor; (C) 13 in ace.rs, kernel_rom.rs, memory.rs with identical source expressions — fingerprints changed because the scoped builder's gate was recomputed using `ChipletSelectors` products. All 18 are semantically equivalent. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/ace.rs#L169) `builder.assert_zero(within_section_gate.clone() * (ptr_next.clone() - expected_ptr_next));` | `ChipletSelectors` struct threaded as parameter. 18 constraints affected in 3 sub-groups: (A) 4 in ace.rs where inline `ace_flag * flag_ace_last` / `ace_flag * flag_ace_next` / `is_transition * ace_flag` products replaced by precomputed `ace_last` / `ace_transition`; (B) 1 in kernel_rom.rs where `when_transition()` folded into the precomputed `flag_next_row_first_kernel_rom` which now includes the transition factor; (C) 13 in ace.rs, kernel_rom.rs, memory.rs with identical source expressions — fingerprints changed because the scoped builder's gate was recomputed using `ChipletSelectors` products. All 18 are semantically equivalent. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/ace.rs#L170) `builder.assert_zero(within_section_gate * (id0.clone() - expected_id0));` | `ChipletSelectors` struct threaded as parameter. 18 constraints affected in 3 sub-groups: (A) 4 in ace.rs where inline `ace_flag * flag_ace_last` / `ace_flag * flag_ace_next` / `is_transition * ace_flag` products replaced by precomputed `ace_last` / `ace_transition`; (B) 1 in kernel_rom.rs where `when_transition()` folded into the precomputed `flag_next_row_first_kernel_rom` which now includes the transition factor; (C) 13 in ace.rs, kernel_rom.rs, memory.rs with identical source expressions — fingerprints changed because the scoped builder's gate was recomputed using `ChipletSelectors` products. All 18 are semantically equivalent. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/ace.rs#L171) `builder.assert_zero(within_section_gate * (id0.clone() - expected_id0));` | `ChipletSelectors` struct threaded as parameter. 18 constraints affected in 3 sub-groups: (A) 4 in ace.rs where inline `ace_flag * flag_ace_last` / `ace_flag * flag_ace_next` / `is_transition * ace_flag` products replaced by precomputed `ace_last` / `ace_transition`; (B) 1 in kernel_rom.rs where `when_transition()` folded into the precomputed `flag_next_row_first_kernel_rom` which now includes the transition factor; (C) 13 in ace.rs, kernel_rom.rs, memory.rs with identical source expressions — fingerprints changed because the scoped builder's gate was recomputed using `ChipletSelectors` products. All 18 are semantically equivalent. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/ace.rs#L188) `builder.assert_zero(ace_transition.clone() * f_read.clone() * (selected - n_eval));` | `ChipletSelectors` struct threaded as parameter. 18 constraints affected in 3 sub-groups: (A) 4 in ace.rs where inline `ace_flag * flag_ace_last` / `ace_flag * flag_ace_next` / `is_transition * ace_flag` products replaced by precomputed `ace_last` / `ace_transition`; (B) 1 in kernel_rom.rs where `when_transition()` folded into the precomputed `flag_next_row_first_kernel_rom` which now includes the transition factor; (C) 13 in ace.rs, kernel_rom.rs, memory.rs with identical source expressions — fingerprints changed because the scoped builder's gate was recomputed using `ChipletSelectors` products. All 18 are semantically equivalent. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/ace.rs#L231) `builder.assert_zero(flag_next_row_first_ace * (sstart_next - F_1));` | `ChipletSelectors` struct threaded as parameter. 18 constraints affected in 3 sub-groups: (A) 4 in ace.rs where inline `ace_flag * flag_ace_last` / `ace_flag * flag_ace_next` / `is_transition * ace_flag` products replaced by precomputed `ace_last` / `ace_transition`; (B) 1 in kernel_rom.rs where `when_transition()` folded into the precomputed `flag_next_row_first_kernel_rom` which now includes the transition factor; (C) 13 in ace.rs, kernel_rom.rs, memory.rs with identical source expressions — fingerprints changed because the scoped builder's gate was recomputed using `ChipletSelectors` products. All 18 are semantically equivalent. |
| [`chiplets/kernel_rom.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/kernel_rom.rs#L92) `builder.assert_eq(r1_next, r1);` | `ChipletSelectors` struct threaded as parameter. 18 constraints affected in 3 sub-groups: (A) 4 in ace.rs where inline `ace_flag * flag_ace_last` / `ace_flag * flag_ace_next` / `is_transition * ace_flag` products replaced by precomputed `ace_last` / `ace_transition`; (B) 1 in kernel_rom.rs where `when_transition()` folded into the precomputed `flag_next_row_first_kernel_rom` which now includes the transition factor; (C) 13 in ace.rs, kernel_rom.rs, memory.rs with identical source expressions — fingerprints changed because the scoped builder's gate was recomputed using `ChipletSelectors` products. All 18 are semantically equivalent. |
| [`chiplets/kernel_rom.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/kernel_rom.rs#L93) `builder.assert_eq(r2_next, r2);` | `ChipletSelectors` struct threaded as parameter. 18 constraints affected in 3 sub-groups: (A) 4 in ace.rs where inline `ace_flag * flag_ace_last` / `ace_flag * flag_ace_next` / `is_transition * ace_flag` products replaced by precomputed `ace_last` / `ace_transition`; (B) 1 in kernel_rom.rs where `when_transition()` folded into the precomputed `flag_next_row_first_kernel_rom` which now includes the transition factor; (C) 13 in ace.rs, kernel_rom.rs, memory.rs with identical source expressions — fingerprints changed because the scoped builder's gate was recomputed using `ChipletSelectors` products. All 18 are semantically equivalent. |
| [`chiplets/kernel_rom.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/kernel_rom.rs#L94) `builder.assert_eq(r3_next, r3);` | `ChipletSelectors` struct threaded as parameter. 18 constraints affected in 3 sub-groups: (A) 4 in ace.rs where inline `ace_flag * flag_ace_last` / `ace_flag * flag_ace_next` / `is_transition * ace_flag` products replaced by precomputed `ace_last` / `ace_transition`; (B) 1 in kernel_rom.rs where `when_transition()` folded into the precomputed `flag_next_row_first_kernel_rom` which now includes the transition factor; (C) 13 in ace.rs, kernel_rom.rs, memory.rs with identical source expressions — fingerprints changed because the scoped builder's gate was recomputed using `ChipletSelectors` products. All 18 are semantically equivalent. |
| [`chiplets/kernel_rom.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/kernel_rom.rs#L95) `builder.assert_eq(r3_next, r3);` | `ChipletSelectors` struct threaded as parameter. 18 constraints affected in 3 sub-groups: (A) 4 in ace.rs where inline `ace_flag * flag_ace_last` / `ace_flag * flag_ace_next` / `is_transition * ace_flag` products replaced by precomputed `ace_last` / `ace_transition`; (B) 1 in kernel_rom.rs where `when_transition()` folded into the precomputed `flag_next_row_first_kernel_rom` which now includes the transition factor; (C) 13 in ace.rs, kernel_rom.rs, memory.rs with identical source expressions — fingerprints changed because the scoped builder's gate was recomputed using `ChipletSelectors` products. All 18 are semantically equivalent. |
| [`chiplets/kernel_rom.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/kernel_rom.rs#L105) `builder.when(flag_next_row_first_kernel_rom).assert_one(sfirst_next);` | `ChipletSelectors` struct threaded as parameter. 18 constraints affected in 3 sub-groups: (A) 4 in ace.rs where inline `ace_flag * flag_ace_last` / `ace_flag * flag_ace_next` / `is_transition * ace_flag` products replaced by precomputed `ace_last` / `ace_transition`; (B) 1 in kernel_rom.rs where `when_transition()` folded into the precomputed `flag_next_row_first_kernel_rom` which now includes the transition factor; (C) 13 in ace.rs, kernel_rom.rs, memory.rs with identical source expressions — fingerprints changed because the scoped builder's gate was recomputed using `ChipletSelectors` products. All 18 are semantically equivalent. |
| [`chiplets/memory.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/memory.rs#L127) `builder.assert_zero(gate.clone() * c1 * cols_next.values[1].clone());` | `ChipletSelectors` struct threaded as parameter. 18 constraints affected in 3 sub-groups: (A) 4 in ace.rs where inline `ace_flag * flag_ace_last` / `ace_flag * flag_ace_next` / `is_transition * ace_flag` products replaced by precomputed `ace_last` / `ace_transition`; (B) 1 in kernel_rom.rs where `when_transition()` folded into the precomputed `flag_next_row_first_kernel_rom` which now includes the transition factor; (C) 13 in ace.rs, kernel_rom.rs, memory.rs with identical source expressions — fingerprints changed because the scoped builder's gate was recomputed using `ChipletSelectors` products. All 18 are semantically equivalent. |
| [`chiplets/memory.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/memory.rs#L128) `builder.assert_zero(gate.clone() * c2 * cols_next.values[2].clone());` | `ChipletSelectors` struct threaded as parameter. 18 constraints affected in 3 sub-groups: (A) 4 in ace.rs where inline `ace_flag * flag_ace_last` / `ace_flag * flag_ace_next` / `is_transition * ace_flag` products replaced by precomputed `ace_last` / `ace_transition`; (B) 1 in kernel_rom.rs where `when_transition()` folded into the precomputed `flag_next_row_first_kernel_rom` which now includes the transition factor; (C) 13 in ace.rs, kernel_rom.rs, memory.rs with identical source expressions — fingerprints changed because the scoped builder's gate was recomputed using `ChipletSelectors` products. All 18 are semantically equivalent. |
| [`chiplets/memory.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/memory.rs#L129) `builder.assert_zero(gate * c3 * cols_next.values[3].clone());` | `ChipletSelectors` struct threaded as parameter. 18 constraints affected in 3 sub-groups: (A) 4 in ace.rs where inline `ace_flag * flag_ace_last` / `ace_flag * flag_ace_next` / `is_transition * ace_flag` products replaced by precomputed `ace_last` / `ace_transition`; (B) 1 in kernel_rom.rs where `when_transition()` folded into the precomputed `flag_next_row_first_kernel_rom` which now includes the transition factor; (C) 13 in ace.rs, kernel_rom.rs, memory.rs with identical source expressions — fingerprints changed because the scoped builder's gate was recomputed using `ChipletSelectors` products. All 18 are semantically equivalent. |
| [`chiplets/memory.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/memory.rs#L130) `builder.assert_zero(gate * c3 * cols_next.values[3].clone());` | `ChipletSelectors` struct threaded as parameter. 18 constraints affected in 3 sub-groups: (A) 4 in ace.rs where inline `ace_flag * flag_ace_last` / `ace_flag * flag_ace_next` / `is_transition * ace_flag` products replaced by precomputed `ace_last` / `ace_transition`; (B) 1 in kernel_rom.rs where `when_transition()` folded into the precomputed `flag_next_row_first_kernel_rom` which now includes the transition factor; (C) 13 in ace.rs, kernel_rom.rs, memory.rs with identical source expressions — fingerprints changed because the scoped builder's gate was recomputed using `ChipletSelectors` products. All 18 are semantically equivalent. |

</details>

#### `39e1e3a01f` refactor: enforce all chiplet selectors are 1 in last row ([diff](https://github.com/0xMiden/miden-vm/compare/94b25974d5...39e1e3a01f))

> 5 added | 465 unchanged

**Added:**

| Expression |
|------------|
| [`chiplets/selectors.rs`](https://github.com/0xMiden/miden-vm/blob/39e1e3a01f/air/src/constraints/chiplets/selectors.rs#L147) `let mut last = builder.when_last_row();` |
| [`chiplets/selectors.rs`](https://github.com/0xMiden/miden-vm/blob/39e1e3a01f/air/src/constraints/chiplets/selectors.rs#L148) `let mut last = builder.when_last_row();` |
| [`chiplets/selectors.rs`](https://github.com/0xMiden/miden-vm/blob/39e1e3a01f/air/src/constraints/chiplets/selectors.rs#L149) `let mut last = builder.when_last_row();` |
| [`chiplets/selectors.rs`](https://github.com/0xMiden/miden-vm/blob/39e1e3a01f/air/src/constraints/chiplets/selectors.rs#L150) `last.assert_one(local.chiplets[4]);` |
| [`chiplets/selectors.rs`](https://github.com/0xMiden/miden-vm/blob/39e1e3a01f/air/src/constraints/chiplets/selectors.rs#L151) `last.assert_one(local.chiplets[4]);` |

#### `fa79579f7d` refactor: remove when_transition from chiplet constraints ([diff](https://github.com/0xMiden/miden-vm/compare/39e1e3a01f...fa79579f7d))

> 61 updated | 409 unchanged

<details>
<summary>Show 61 changes</summary>

**Updated:**

| Expression | Remark |
|------------|--------|
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/ace.rs#L144) `builder.assert_zero(ace_flag.clone() * f_end.clone() * sblock.not());` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/ace.rs#L211) `builder.assert_zero(gate.clone() * v0_1);` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/ace.rs#L212) `builder.assert_zero(gate * id0);` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/ace.rs#L213) `builder.assert_zero(gate * id0);` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L70) `builder.assert_zero(gate * (b.square() - b.clone()));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L80) `builder.assert_zero(gate * (cols_next.node_index.clone() - cols.node_index.clone()));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L120) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_absorb.clone() * cap_next[i].clone() }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L120) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_absorb.clone() * cap_next[i].clone() }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L120) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_absorb.clone() * cap_next[i].clone() }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L120) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_absorb.clone() * cap_next[i].clone() }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L131) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_b0.clone() * (rate0_next[i].clone() - digest[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L131) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_b0.clone() * (rate0_next[i].clone() - digest[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L131) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_b0.clone() * (rate0_next[i].clone() - digest[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L131) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_b0.clone() * (rate0_next[i].clone() - digest[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L138) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_b1.clone() * (rate1_next[i].clone() - digest[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L138) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_b1.clone() * (rate1_next[i].clone() - digest[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L138) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_b1.clone() * (rate1_next[i].clone() - digest[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L138) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_b1.clone() * (rate1_next[i].clone() - digest[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/selectors.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/selectors.rs#L66) `builder.assert_zeros([ gate.clone() * (cols_next.s1.clone() - cols.s1.clone()), gate * (cols_next.s2.clone() - cols.s2.clone()), ]);` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/selectors.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/selectors.rs#L66) `builder.assert_zeros([ gate.clone() * (cols_next.s1.clone() - cols.s1.clone()), gate * (cols_next.s2.clone() - cols.s2.clone()), ]);` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/selectors.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/selectors.rs#L74) `builder.assert_zero(gate * cols_next.s0.clone());` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L113) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate.clone() * (h_cap_next[i].clone() - h_cap[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L113) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate.clone() * (h_cap_next[i].clone() - h_cap[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L113) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate.clone() * (h_cap_next[i].clone() - h_cap[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L113) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate.clone() * (h_cap_next[i].clone() - h_cap[i].clone()) }));` | `when_transition()` guard removed from all chiplet constraints. 61 constraints across ace.rs (4), hasher/merkle.rs (14), hasher/selectors.rs (3), hasher/state.rs (36 — init/ext/int permutation steps × 12 lanes + 4 ABP capacity). Each constraint polynomial lost its `(1 − z_last)` factor. Soundness preserved because `39e1e3a01f` enforces all selectors = 1 on the last row → every chiplet `is_active` flag vanishes there → constraints auto-zero without the explicit transition guard. Polynomially non-equivalent but semantically equivalent under the last-row invariant. |

</details>

#### `7a366bde1f` refactor: add per-bus domain separation to Challenges encoding ([diff](https://github.com/0xMiden/miden-vm/compare/fa79579f7d...7a366bde1f))

> 8 updated | 462 unchanged

**Updated:**

| Expression | Remark |
|------------|--------|
| [`chiplets/bus/chiplets.rs`](https://github.com/0xMiden/miden-vm/blob/7a366bde1f/air/src/constraints/chiplets/bus/chiplets.rs#L331) `builder.when_transition().assert_eq_ext(lhs, rhs);` | **Intentional non-equivalent change.** Per-bus domain tag added to `Challenges` encoding. 8 bus constraints affected: chiplets bus (1), hash_kernel bus (1), wiring bus (1), decoder bus (3), range bus (1), stack bus (1). Each polynomial now includes a distinct domain separator constant. Source expressions are identical before/after — the change is in how `Challenges` computes the encoding, not in the assertion code. |
| [`chiplets/bus/hash_kernel.rs`](https://github.com/0xMiden/miden-vm/blob/7a366bde1f/air/src/constraints/chiplets/bus/hash_kernel.rs#L275) `builder .when_transition() .assert_eq_ext(p_next_ef * requests, p_local_ef * responses);` | **Intentional non-equivalent change.** Per-bus domain tag added to `Challenges` encoding. 8 bus constraints affected: chiplets bus (1), hash_kernel bus (1), wiring bus (1), decoder bus (3), range bus (1), stack bus (1). Each polynomial now includes a distinct domain separator constant. Source expressions are identical before/after — the change is in how `Challenges` computes the encoding, not in the assertion code. |
| [`chiplets/bus/wiring.rs`](https://github.com/0xMiden/miden-vm/blob/7a366bde1f/air/src/constraints/chiplets/bus/wiring.rs#L148) `builder.when_transition().assert_zero_ext(wiring_constraint);` | **Intentional non-equivalent change.** Per-bus domain tag added to `Challenges` encoding. 8 bus constraints affected: chiplets bus (1), hash_kernel bus (1), wiring bus (1), decoder bus (3), range bus (1), stack bus (1). Each polynomial now includes a distinct domain separator constant. Source expressions are identical before/after — the change is in how `Challenges` computes the encoding, not in the assertion code. |
| [`decoder/bus.rs`](https://github.com/0xMiden/miden-vm/blob/7a366bde1f/air/src/constraints/decoder/bus.rs#L441) `builder.when_transition().assert_eq_ext(lhs, rhs);` | **Intentional non-equivalent change.** Per-bus domain tag added to `Challenges` encoding. 8 bus constraints affected: chiplets bus (1), hash_kernel bus (1), wiring bus (1), decoder bus (3), range bus (1), stack bus (1). Each polynomial now includes a distinct domain separator constant. Source expressions are identical before/after — the change is in how `Challenges` computes the encoding, not in the assertion code. |
| [`decoder/bus.rs`](https://github.com/0xMiden/miden-vm/blob/7a366bde1f/air/src/constraints/decoder/bus.rs#L667) `builder.when_transition().assert_eq_ext(lhs, rhs);` | **Intentional non-equivalent change.** Per-bus domain tag added to `Challenges` encoding. 8 bus constraints affected: chiplets bus (1), hash_kernel bus (1), wiring bus (1), decoder bus (3), range bus (1), stack bus (1). Each polynomial now includes a distinct domain separator constant. Source expressions are identical before/after — the change is in how `Challenges` computes the encoding, not in the assertion code. |
| [`decoder/bus.rs`](https://github.com/0xMiden/miden-vm/blob/7a366bde1f/air/src/constraints/decoder/bus.rs#L861) `builder.when_transition().assert_eq_ext(lhs, rhs);` | **Intentional non-equivalent change.** Per-bus domain tag added to `Challenges` encoding. 8 bus constraints affected: chiplets bus (1), hash_kernel bus (1), wiring bus (1), decoder bus (3), range bus (1), stack bus (1). Each polynomial now includes a distinct domain separator constant. Source expressions are identical before/after — the change is in how `Challenges` computes the encoding, not in the assertion code. |
| [`range/bus.rs`](https://github.com/0xMiden/miden-vm/blob/7a366bde1f/air/src/constraints/range/bus.rs#L129) `builder.when_transition().assert_zero_ext( b_next_term - b_term - rc_term + s0_term + s1_term + s2_term + s3_term + m0_term + m1_term, );` | **Intentional non-equivalent change.** Per-bus domain tag added to `Challenges` encoding. 8 bus constraints affected: chiplets bus (1), hash_kernel bus (1), wiring bus (1), decoder bus (3), range bus (1), stack bus (1). Each polynomial now includes a distinct domain separator constant. Source expressions are identical before/after — the change is in how `Challenges` computes the encoding, not in the assertion code. |
| [`stack/bus.rs`](https://github.com/0xMiden/miden-vm/blob/7a366bde1f/air/src/constraints/stack/bus.rs#L135) `builder.when_transition().assert_eq_ext(lhs, rhs);` | **Intentional non-equivalent change.** Per-bus domain tag added to `Challenges` encoding. 8 bus constraints affected: chiplets bus (1), hash_kernel bus (1), wiring bus (1), decoder bus (3), range bus (1), stack bus (1). Each polynomial now includes a distinct domain separator constant. Source expressions are identical before/after — the change is in how `Challenges` computes the encoding, not in the assertion code. |

#### `c14fd13f56` refactor: inline small constraint helper functions ([diff](https://github.com/0xMiden/miden-vm/compare/59565a12ef...c14fd13f56))

> 1 updated | 469 unchanged

**Updated:**

| Expression | Remark |
|------------|--------|
| [`decoder/mod.rs`](https://github.com/0xMiden/miden-vm/blob/c14fd13f56/air/src/constraints/decoder/mod.rs#L132) `builder.assert_one(sp + op_flags.control_flow());` | Helper `enforce_decoder_selector` inlined into call site. Expression restructured: `assert_zero(ONE - sp - ctrl_flag)` → `assert_one(sp + control_flow())`. Polynomial sign-flipped (`-(1 - sp - ctrl)` vs `sp + ctrl - 1`) but both assert the same zero. Moved from decoder/mod.rs:699 to decoder/mod.rs:132. |

#### `0216cd69de` refactor: apply constraint style rules — when() decomposition, semantic assertions ([diff](https://github.com/0xMiden/miden-vm/compare/e370da6abf...0216cd69de))

> 10 updated | 460 unchanged

**Updated:**

| Expression | Remark |
|------------|--------|
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/0216cd69de/air/src/constraints/chiplets/ace.rs#L132) `.assert_one(sblock_next.clone());` | ace.rs:121→132. Gate extraction: `assert_zero(ace_transition * f_next * sblock * sblock_next.not())` → `when(ace_transition).when(f_next).when(sblock).assert_one(sblock_next)`. Sign flip + compound gate decomposed into `when()` chain. Equivalent. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/0216cd69de/air/src/constraints/chiplets/ace.rs#L136) `builder.when(ace_flag.clone()).when(f_end.clone()).assert_one(sblock.clone());` | ace.rs:125→136. Gate extraction: `assert_zero(ace_flag * f_end * sblock.not())` → `when(ace_flag).when(f_end).assert_one(sblock)`. Sign flip + gate decomposition. Equivalent. |
| [`decoder/mod.rs`](https://github.com/0xMiden/miden-vm/blob/0216cd69de/air/src/constraints/decoder/mod.rs#L129) `builder.when_transition().when(span_flag).assert_one(sp_next.clone());` | decoder:129. Sign flip: `when_transition().assert_zero(span_flag * sp_next.not())` → `when_transition().when(span_flag).assert_one(sp_next)`. Equivalent. |
| [`decoder/mod.rs`](https://github.com/0xMiden/miden-vm/blob/0216cd69de/air/src/constraints/decoder/mod.rs#L133) `builder.when_transition().when(respan_flag).assert_one(sp_next);` | decoder:133. Sign flip: `when_transition().assert_zero(respan_flag * sp_next.not())` → `when_transition().when(respan_flag).assert_one(sp_next)`. Equivalent. |
| [`decoder/mod.rs`](https://github.com/0xMiden/miden-vm/blob/0216cd69de/air/src/constraints/decoder/mod.rs#L225) `builder.assert_one(end_flags.is_loop_body);` | decoder:217→225. Gate extraction into `when(f_repeat)` scope: `assert_zero(f_repeat * s0.not())` → `when(f_repeat).assert_one(s0)`. Sign flip + gate moved to scoped builder. Equivalent. |
| [`decoder/mod.rs`](https://github.com/0xMiden/miden-vm/blob/0216cd69de/air/src/constraints/decoder/mod.rs#L226) `builder.assert_one(end_flags.is_loop_body);` | decoder:218→226. Gate extraction into `when(f_repeat)` scope: `assert_zero(f_repeat * is_loop_body.not())` → `when(f_repeat).assert_one(is_loop_body)`. Sign flip + gate moved to scoped builder. Equivalent. |
| [`decoder/mod.rs`](https://github.com/0xMiden/miden-vm/blob/0216cd69de/air/src/constraints/decoder/mod.rs#L245) `builder.when_transition().when(f_halt).assert_one(f_halt_next);` | decoder:237→245. Sign flip: `when_transition().assert_zero(f_halt * f_halt_next.not())` → `when_transition().when(f_halt).assert_one(f_halt_next)`. Equivalent. |
| [`stack/ops/mod.rs`](https://github.com/0xMiden/miden-vm/blob/0216cd69de/air/src/constraints/stack/ops/mod.rs#L227) `builder.when(is_cswap.clone()).assert_bool(cswap_c);` | stack/ops:313→227 (CSWAP). Gained `is_transition` factor: gate `builder.is_transition() * is_cswap` was manually computed, now `when_transition()` hoisted to function scope. Polynomial restructured but algebraically identical — `is_transition * is_cswap` present in both. Equivalent. |
| [`stack/ops/mod.rs`](https://github.com/0xMiden/miden-vm/blob/0216cd69de/air/src/constraints/stack/ops/mod.rs#L237) `builder.when(is_cswapw.clone()).assert_bool(cswap_c);` | stack/ops:324→237 (CSWAPW). Gained `is_transition` factor: same restructuring as CSWAP — manually computed `builder.is_transition() * is_cswapw` gate replaced by function-level `when_transition()`. Algebraically identical. Equivalent. |
| [`stack/ops/mod.rs`](https://github.com/0xMiden/miden-vm/blob/0216cd69de/air/src/constraints/stack/ops/mod.rs#L253) `builder.when(is_assert).assert_one(s0);` | stack/ops:341→253 (ASSERT). **Gained `is_transition` factor** not present before. Before: `when(is_assert).assert_one(s0)` (all rows). After: `when_transition().when(is_assert).assert_one(s0)` (transition rows only). Semantically equivalent because `is_assert` is an op flag that is zero on the last row (no operation executes there), so the constraint was already vacuous on the last row. |
