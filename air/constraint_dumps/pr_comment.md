<!-- constraint-equivalence-report -->

## Constraint Equivalence Report

**Last updated:** 2026-03-24 16:38 UTC  
**Range:** `f7c294c441..ac2a1ba98b` (45 commits)

### Summary

| Metric | Value |
|--------|-------|
| Commits analyzed | 45 |
| Pairs with changes | **6** |
| Pairs verified equivalent | 38 |
| Total constraints updated | 98 |
| Total constraints added | 5 |

### Changes

#### `94b25974d5` refactor: thread ChipletSelectors through all constraint and bus functions ([diff](https://github.com/0xMiden/miden-vm/compare/1e23a431e2...94b25974d5))

> 18 updated | 447 unchanged

<details>
<summary>Show 18 changes</summary>

**Updated:**

| Expression | Remark |
|------------|--------|
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/ace.rs#L136) `builder.assert_zero(ace_last.clone() * sstart.clone());` | `ChipletSelectors` struct now threaded as parameter; selector access changed from field lookup to method call, shifting line numbers. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/ace.rs#L138) `builder.assert_zero(ace_transition.clone() * sstart.clone() * sstart_next.clone());` | `ChipletSelectors` struct now threaded as parameter; selector access changed from field lookup to method call, shifting line numbers. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/ace.rs#L142) `builder.assert_zero( ace_transition.clone() * f_next.clone() * sblock.clone() * sblock_next.not(), );` | `ChipletSelectors` struct now threaded as parameter; selector access changed from field lookup to method call, shifting line numbers. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/ace.rs#L168) `builder.assert_zero(within_section_gate.clone() * (clk_next.clone() - clk.clone()));` | `ChipletSelectors` struct now threaded as parameter; selector access changed from field lookup to method call, shifting line numbers. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/ace.rs#L169) `builder.assert_zero(within_section_gate.clone() * (ptr_next.clone() - expected_ptr_next));` | `ChipletSelectors` struct now threaded as parameter; selector access changed from field lookup to method call, shifting line numbers. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/ace.rs#L170) `builder.assert_zero(within_section_gate * (id0.clone() - expected_id0));` | `ChipletSelectors` struct now threaded as parameter; selector access changed from field lookup to method call, shifting line numbers. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/ace.rs#L171) `builder.assert_zero(within_section_gate * (id0.clone() - expected_id0));` | `ChipletSelectors` struct now threaded as parameter; selector access changed from field lookup to method call, shifting line numbers. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/ace.rs#L188) `builder.assert_zero(ace_transition.clone() * f_read.clone() * (selected - n_eval));` | `ChipletSelectors` struct now threaded as parameter; selector access changed from field lookup to method call, shifting line numbers. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/ace.rs#L231) `builder.assert_zero(flag_next_row_first_ace * (sstart_next - F_1));` | `ChipletSelectors` struct now threaded as parameter; selector access changed from field lookup to method call, shifting line numbers. |
| [`chiplets/kernel_rom.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/kernel_rom.rs#L92) `builder.assert_eq(r1_next, r1);` | `ChipletSelectors` struct now threaded as parameter; selector access changed from field lookup to method call, shifting line numbers. |
| [`chiplets/kernel_rom.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/kernel_rom.rs#L93) `builder.assert_eq(r2_next, r2);` | `ChipletSelectors` struct now threaded as parameter; selector access changed from field lookup to method call, shifting line numbers. |
| [`chiplets/kernel_rom.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/kernel_rom.rs#L94) `builder.assert_eq(r3_next, r3);` | `ChipletSelectors` struct now threaded as parameter; selector access changed from field lookup to method call, shifting line numbers. |
| [`chiplets/kernel_rom.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/kernel_rom.rs#L95) `builder.assert_eq(r3_next, r3);` | `ChipletSelectors` struct now threaded as parameter; selector access changed from field lookup to method call, shifting line numbers. |
| [`chiplets/kernel_rom.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/kernel_rom.rs#L105) `builder.when(flag_next_row_first_kernel_rom).assert_one(sfirst_next);` | `ChipletSelectors` struct now threaded as parameter; selector access changed from field lookup to method call, shifting line numbers. |
| [`chiplets/memory.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/memory.rs#L127) `builder.assert_zero(gate.clone() * c1 * cols_next.values[1].clone());` | `ChipletSelectors` struct now threaded as parameter; selector access changed from field lookup to method call, shifting line numbers. |
| [`chiplets/memory.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/memory.rs#L128) `builder.assert_zero(gate.clone() * c2 * cols_next.values[2].clone());` | `ChipletSelectors` struct now threaded as parameter; selector access changed from field lookup to method call, shifting line numbers. |
| [`chiplets/memory.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/memory.rs#L129) `builder.assert_zero(gate * c3 * cols_next.values[3].clone());` | `ChipletSelectors` struct now threaded as parameter; selector access changed from field lookup to method call, shifting line numbers. |
| [`chiplets/memory.rs`](https://github.com/0xMiden/miden-vm/blob/94b25974d5/air/src/constraints/chiplets/memory.rs#L130) `builder.assert_zero(gate * c3 * cols_next.values[3].clone());` | `ChipletSelectors` struct now threaded as parameter; selector access changed from field lookup to method call, shifting line numbers. |

</details>

#### `39e1e3a01f` refactor: enforce all chiplet selectors are 1 in last row ([diff](https://github.com/0xMiden/miden-vm/compare/94b25974d5...39e1e3a01f))

> 5 added | 465 unchanged

**Added:**

| Expression |
|------------|
| [`chiplets/selectors.rs`](https://github.com/0xMiden/miden-vm/blob/39e1e3a01f/air/src/constraints/chiplets/selectors.rs#L147) `let mut last = builder.when_last_row();` |
| [`chiplets/selectors.rs`](https://github.com/0xMiden/miden-vm/blob/39e1e3a01f/air/src/constraints/chiplets/selectors.rs#L148) `let mut last = builder.when_last_row();` |
| [`chiplets/selectors.rs`](https://github.com/0xMiden/miden-vm/blob/39e1e3a01f/air/src/constraints/chiplets/selectors.rs#L149) `let mut last = builder.when_last_row();` |
| [`chiplets/selectors.rs`](https://github.com/0xMiden/miden-vm/blob/39e1e3a01f/air/src/constraints/chiplets/selectors.rs#L150) `last.assert_one(local.chiplets[4]);` |
| [`chiplets/selectors.rs`](https://github.com/0xMiden/miden-vm/blob/39e1e3a01f/air/src/constraints/chiplets/selectors.rs#L151) `last.assert_one(local.chiplets[4]);` |

#### `fa79579f7d` refactor: remove when_transition from chiplet constraints ([diff](https://github.com/0xMiden/miden-vm/compare/39e1e3a01f...fa79579f7d))

> 61 updated | 409 unchanged

<details>
<summary>Show 61 changes</summary>

**Updated:**

| Expression | Remark |
|------------|--------|
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/ace.rs#L144) `builder.assert_zero(ace_flag.clone() * f_end.clone() * sblock.not());` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/ace.rs#L211) `builder.assert_zero(gate.clone() * v0_1);` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/ace.rs#L212) `builder.assert_zero(gate * id0);` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/ace.rs#L213) `builder.assert_zero(gate * id0);` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L70) `builder.assert_zero(gate * (b.square() - b.clone()));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L80) `builder.assert_zero(gate * (cols_next.node_index.clone() - cols.node_index.clone()));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L120) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_absorb.clone() * cap_next[i].clone() }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L120) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_absorb.clone() * cap_next[i].clone() }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L120) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_absorb.clone() * cap_next[i].clone() }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L120) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_absorb.clone() * cap_next[i].clone() }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L131) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_b0.clone() * (rate0_next[i].clone() - digest[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L131) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_b0.clone() * (rate0_next[i].clone() - digest[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L131) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_b0.clone() * (rate0_next[i].clone() - digest[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L131) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_b0.clone() * (rate0_next[i].clone() - digest[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L138) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_b1.clone() * (rate1_next[i].clone() - digest[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L138) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_b1.clone() * (rate1_next[i].clone() - digest[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L138) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_b1.clone() * (rate1_next[i].clone() - digest[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/merkle.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/merkle.rs#L138) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate_b1.clone() * (rate1_next[i].clone() - digest[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/selectors.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/selectors.rs#L66) `builder.assert_zeros([ gate.clone() * (cols_next.s1.clone() - cols.s1.clone()), gate * (cols_next.s2.clone() - cols.s2.clone()), ]);` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/selectors.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/selectors.rs#L66) `builder.assert_zeros([ gate.clone() * (cols_next.s1.clone() - cols.s1.clone()), gate * (cols_next.s2.clone() - cols.s2.clone()), ]);` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/selectors.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/selectors.rs#L74) `builder.assert_zero(gate * cols_next.s0.clone());` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L83) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_init.clone() * (h_next[i].clone() - expected_init[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L88) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_ext.clone() * (h_next[i].clone() - expected_ext[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L93) `builder.assert_zeros(core::array::from_fn::<_, STATE_WIDTH, _>(\|i\| { gate_int.clone() * (h_next[i].clone() - expected_int[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L113) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate.clone() * (h_cap_next[i].clone() - h_cap[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L113) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate.clone() * (h_cap_next[i].clone() - h_cap[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L113) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate.clone() * (h_cap_next[i].clone() - h_cap[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |
| [`chiplets/hasher/state.rs`](https://github.com/0xMiden/miden-vm/blob/fa79579f7d/air/src/constraints/chiplets/hasher/state.rs#L113) `builder.assert_zeros(core::array::from_fn::<_, 4, _>(\|i\| { gate.clone() * (h_cap_next[i].clone() - h_cap[i].clone()) }));` | `when_transition()` guard removed; transition gate now multiplied directly into each constraint expression. |

</details>

#### `7a366bde1f` refactor: add per-bus domain separation to Challenges encoding ([diff](https://github.com/0xMiden/miden-vm/compare/fa79579f7d...7a366bde1f))

> 8 updated | 462 unchanged

**Updated:**

| Expression | Remark |
|------------|--------|
| [`chiplets/bus/chiplets.rs`](https://github.com/0xMiden/miden-vm/blob/7a366bde1f/air/src/constraints/chiplets/bus/chiplets.rs#L331) `builder.when_transition().assert_eq_ext(lhs, rhs);` | Per-bus domain tag added to `Challenges` encoding; each bus constraint now includes a distinct domain separator. |
| [`chiplets/bus/hash_kernel.rs`](https://github.com/0xMiden/miden-vm/blob/7a366bde1f/air/src/constraints/chiplets/bus/hash_kernel.rs#L275) `builder .when_transition() .assert_eq_ext(p_next_ef * requests, p_local_ef * responses);` | Per-bus domain tag added to `Challenges` encoding; each bus constraint now includes a distinct domain separator. |
| [`chiplets/bus/wiring.rs`](https://github.com/0xMiden/miden-vm/blob/7a366bde1f/air/src/constraints/chiplets/bus/wiring.rs#L148) `builder.when_transition().assert_zero_ext(wiring_constraint);` | Per-bus domain tag added to `Challenges` encoding; each bus constraint now includes a distinct domain separator. |
| [`decoder/bus.rs`](https://github.com/0xMiden/miden-vm/blob/7a366bde1f/air/src/constraints/decoder/bus.rs#L441) `builder.when_transition().assert_eq_ext(lhs, rhs);` | Per-bus domain tag added to `Challenges` encoding; each bus constraint now includes a distinct domain separator. |
| [`decoder/bus.rs`](https://github.com/0xMiden/miden-vm/blob/7a366bde1f/air/src/constraints/decoder/bus.rs#L667) `builder.when_transition().assert_eq_ext(lhs, rhs);` | Per-bus domain tag added to `Challenges` encoding; each bus constraint now includes a distinct domain separator. |
| [`decoder/bus.rs`](https://github.com/0xMiden/miden-vm/blob/7a366bde1f/air/src/constraints/decoder/bus.rs#L861) `builder.when_transition().assert_eq_ext(lhs, rhs);` | Per-bus domain tag added to `Challenges` encoding; each bus constraint now includes a distinct domain separator. |
| [`range/bus.rs`](https://github.com/0xMiden/miden-vm/blob/7a366bde1f/air/src/constraints/range/bus.rs#L129) `builder.when_transition().assert_zero_ext( b_next_term - b_term - rc_term + s0_term + s1_term + s2_term + s3_term + m0_term + m1_term, );` | Per-bus domain tag added to `Challenges` encoding; each bus constraint now includes a distinct domain separator. |
| [`stack/bus.rs`](https://github.com/0xMiden/miden-vm/blob/7a366bde1f/air/src/constraints/stack/bus.rs#L135) `builder.when_transition().assert_eq_ext(lhs, rhs);` | Per-bus domain tag added to `Challenges` encoding; each bus constraint now includes a distinct domain separator. |

#### `c14fd13f56` refactor: inline small constraint helper functions ([diff](https://github.com/0xMiden/miden-vm/compare/59565a12ef...c14fd13f56))

> 1 updated | 469 unchanged

**Updated:**

| Expression | Remark |
|------------|--------|
| [`decoder/mod.rs`](https://github.com/0xMiden/miden-vm/blob/c14fd13f56/air/src/constraints/decoder/mod.rs#L132) `builder.assert_one(sp + op_flags.control_flow());` | Small helper function inlined; constraint expression unchanged, only call-site location moved. |

#### `ac2a1ba98b`  ([diff](https://github.com/0xMiden/miden-vm/compare/e370da6abf...ac2a1ba98b))

> 10 updated | 460 unchanged

**Updated:**

| Expression | Remark |
|------------|--------|
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/ac2a1ba98b/air/src/constraints/chiplets/ace.rs#L132) `.assert_one(sblock_next.clone());` | Constraint style refactor: gates factored into when() chains, semantic assertions applied. 7 sign flips (assert_zero(g*(1-x)) → when(g).assert_one(x)) and 3 constraints gained is_transition factor (op flags vanish on last row, so constraints were already vacuous there). All changes are semantically equivalent. |
| [`chiplets/ace.rs`](https://github.com/0xMiden/miden-vm/blob/ac2a1ba98b/air/src/constraints/chiplets/ace.rs#L136) `builder.when(ace_flag.clone()).when(f_end.clone()).assert_one(sblock.clone());` | Constraint style refactor: gates factored into when() chains, semantic assertions applied. 7 sign flips (assert_zero(g*(1-x)) → when(g).assert_one(x)) and 3 constraints gained is_transition factor (op flags vanish on last row, so constraints were already vacuous there). All changes are semantically equivalent. |
| [`decoder/mod.rs`](https://github.com/0xMiden/miden-vm/blob/ac2a1ba98b/air/src/constraints/decoder/mod.rs#L129) `builder.when_transition().when(span_flag).assert_one(sp_next.clone());` | Constraint style refactor: gates factored into when() chains, semantic assertions applied. 7 sign flips (assert_zero(g*(1-x)) → when(g).assert_one(x)) and 3 constraints gained is_transition factor (op flags vanish on last row, so constraints were already vacuous there). All changes are semantically equivalent. |
| [`decoder/mod.rs`](https://github.com/0xMiden/miden-vm/blob/ac2a1ba98b/air/src/constraints/decoder/mod.rs#L133) `builder.when_transition().when(respan_flag).assert_one(sp_next);` | Constraint style refactor: gates factored into when() chains, semantic assertions applied. 7 sign flips (assert_zero(g*(1-x)) → when(g).assert_one(x)) and 3 constraints gained is_transition factor (op flags vanish on last row, so constraints were already vacuous there). All changes are semantically equivalent. |
| [`decoder/mod.rs`](https://github.com/0xMiden/miden-vm/blob/ac2a1ba98b/air/src/constraints/decoder/mod.rs#L225) `builder.assert_one(end_flags.is_loop_body);` | Constraint style refactor: gates factored into when() chains, semantic assertions applied. 7 sign flips (assert_zero(g*(1-x)) → when(g).assert_one(x)) and 3 constraints gained is_transition factor (op flags vanish on last row, so constraints were already vacuous there). All changes are semantically equivalent. |
| [`decoder/mod.rs`](https://github.com/0xMiden/miden-vm/blob/ac2a1ba98b/air/src/constraints/decoder/mod.rs#L226) `builder.assert_one(end_flags.is_loop_body);` | Constraint style refactor: gates factored into when() chains, semantic assertions applied. 7 sign flips (assert_zero(g*(1-x)) → when(g).assert_one(x)) and 3 constraints gained is_transition factor (op flags vanish on last row, so constraints were already vacuous there). All changes are semantically equivalent. |
| [`decoder/mod.rs`](https://github.com/0xMiden/miden-vm/blob/ac2a1ba98b/air/src/constraints/decoder/mod.rs#L245) `builder.when_transition().when(f_halt).assert_one(f_halt_next);` | Constraint style refactor: gates factored into when() chains, semantic assertions applied. 7 sign flips (assert_zero(g*(1-x)) → when(g).assert_one(x)) and 3 constraints gained is_transition factor (op flags vanish on last row, so constraints were already vacuous there). All changes are semantically equivalent. |
| [`stack/ops/mod.rs`](https://github.com/0xMiden/miden-vm/blob/ac2a1ba98b/air/src/constraints/stack/ops/mod.rs#L227) `builder.when(is_cswap.clone()).assert_bool(cswap_c);` | Constraint style refactor: gates factored into when() chains, semantic assertions applied. 7 sign flips (assert_zero(g*(1-x)) → when(g).assert_one(x)) and 3 constraints gained is_transition factor (op flags vanish on last row, so constraints were already vacuous there). All changes are semantically equivalent. |
| [`stack/ops/mod.rs`](https://github.com/0xMiden/miden-vm/blob/ac2a1ba98b/air/src/constraints/stack/ops/mod.rs#L237) `builder.when(is_cswapw.clone()).assert_bool(cswap_c);` | Constraint style refactor: gates factored into when() chains, semantic assertions applied. 7 sign flips (assert_zero(g*(1-x)) → when(g).assert_one(x)) and 3 constraints gained is_transition factor (op flags vanish on last row, so constraints were already vacuous there). All changes are semantically equivalent. |
| [`stack/ops/mod.rs`](https://github.com/0xMiden/miden-vm/blob/ac2a1ba98b/air/src/constraints/stack/ops/mod.rs#L253) `builder.when(is_assert).assert_one(s0);` | Constraint style refactor: gates factored into when() chains, semantic assertions applied. 7 sign flips (assert_zero(g*(1-x)) → when(g).assert_one(x)) and 3 constraints gained is_transition factor (op flags vanish on last row, so constraints were already vacuous there). All changes are semantically equivalent. |
